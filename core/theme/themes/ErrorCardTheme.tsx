import createTheme from "@/core/theme/createTheme";

const ErrorCardTheme = createTheme("ErrorCard", {
  color: "errorContrastColor",
  backgroundColor: "errorColor",
  style: "cardStyle",
});

export default ErrorCardTheme;
