import createTheme from "@/core/theme/createTheme";

const AccentCardTheme = createTheme("AccentCard", {
  color: "accentContrastColor",
  backgroundColor: "accentColor",
  style: "cardStyle",
});

export default AccentCardTheme;
