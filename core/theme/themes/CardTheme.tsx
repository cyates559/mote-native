import createTheme from "@/core/theme/createTheme";

const CardTheme = createTheme("Card", {
  color: "cardColor",
  backgroundColor: "cardBackgroundColor",
  style: "cardStyle",
});

export default CardTheme;
