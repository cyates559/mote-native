import createTheme from "@/core/theme/createTheme";

const MenuTheme = createTheme("Menu", {
  color: "menuColor",
  backgroundColor: "menuBackgroundColor",
  style: "menuStyle",
});

export default MenuTheme;
