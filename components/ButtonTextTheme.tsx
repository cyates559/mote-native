import {ReactNode} from "react";
import {Theme} from "@/styled";
import {ButtonStylePropsType} from "./Button";

export default function ButtonTextTheme({accent, filled, ...rest}: ButtonStylePropsType & {children: ReactNode}) {
  const
  return <Theme {...rest}/>
}